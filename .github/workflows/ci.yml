name: CI

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format:
    name: Format PR4 Source and Tests
    runs-on: ubuntu-latest

    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
        with:
          ref: agent/pr4-enterprise-review-workspace

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Apply repository formatting
        run: |
          npm ci
          npx prettier \
            apps/frontend/src/features/generic-extraction/generic-document-review.tsx \
            apps/frontend/test/App.test.tsx \
            apps/frontend/test/generic-document-api.test.ts \
            apps/frontend/test/generic-document-file.test.ts \
            apps/frontend/test/generic-document-review.test.tsx \
            apps/frontend/test/generic-extraction-workspace.test.tsx \
            apps/frontend/test/support/generic-document.ts \
            --write
          git add apps/frontend/src/features/generic-extraction/generic-document-review.tsx apps/frontend/test
          if git diff --cached --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m "style: format enterprise workspace tests"
          git push origin HEAD:agent/pr4-enterprise-review-workspace
