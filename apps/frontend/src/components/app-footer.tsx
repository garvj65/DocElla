export function AppFooter() {
  return (
    <footer className="border-t border-[var(--color-border)]">
      <div className="mx-auto max-w-6xl px-4 py-5 text-sm text-[var(--color-muted)] sm:px-6">
        DocElla extracts, reviews, validates, and generates PDFs without persistent document storage.
      </div>
    </footer>
  );
}
